# microservice specific routes

->          /govuk-frontend                           govuk.Routes
->          /hmrc-frontend                            hmrcfrontend.Routes

GET         /start/:journeyId                         bankaccountverification.web.VerificationController.start(journeyId)
POST        /verify/:journeyId                        bankaccountverification.web.VerificationController.verifyDetails(journeyId)

GET         /assets/*file                             controllers.Assets.versioned(path = "/public", file: Asset)
