# Add all the application routes to the app.routes file

->          /api                            api.Routes
->          /bank-account-verification      app.Routes
->          /                               health.Routes
